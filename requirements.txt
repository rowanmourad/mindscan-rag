# Core data handling
pandas
numpy>=1.26,<2.2
# Web UI
streamlit
# PDF text extraction
pypdf
# Embeddings + vector database
# NOTE: --extra-index-url pulls the CPU-only torch build (no bundled CUDA/
# NVIDIA native libraries). Plain "torch" from PyPI is the GPU build by
# default and drags in nvidia-cublas/nvidia-cudnn/cuda-toolkit etc. even on
# this GPU-less deployment -- those unused CUDA libraries initializing (or
# failing to) alongside tensorflow-cpu in the same process is the most
# likely cause of the startup segmentation fault.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
sentence-transformers
chromadb
rank-bm25
# Faster HuggingFace model downloads (optional but recommended)
hf_transfer
# Classical ML utilities (used by the braintumor package / preprocessing)
scikit-learn
opencv-python-headless
matplotlib
# HTTP calls to the OpenRouter API
requests
# PDF report generation
reportlab
# Tumor-classification models (loaded via the braintumor package)
tensorflow-cpu==2.21.0
keras==3.15.0
